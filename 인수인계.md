# 와플트립 인수인계

> ## 2026-09-02 현재 — 다음 주에 여기서 이어받는다
>
> 사이트는 **https://peoplay-k.github.io/waffletrip/** 에서 돌고 있고,
> 매일 05:00 KST 에 자동으로 수집·편집·발행·배포된다. **손을 놓아도 신문은 나간다.**
>
> ### 다음 주에 할 일 — 순서가 중요하다
>
> **1. 카페24 DNS (사장님만 가능)**
> 지금 검색엔진이 보는 주소가 `peoplay-k.github.io/waffletrip/` 다.
> 이 상태로 서치콘솔에 등록하고 색인이 쌓이면 나중에 도메인을 옮길 때
> 처음부터 다시 해야 한다. **DNS 를 먼저 붙여야 나머지가 안 꼬인다.**
>
> | 타입 | 호스트 | 값 |
> |---|---|---|
> | A | @ | 185.199.108.153 |
> | A | @ | 185.199.109.153 |
> | A | @ | 185.199.110.153 |
> | A | @ | 185.199.111.153 |
> | CNAME | www | peoplay-k.github.io |
>
> DNS 가 붙은 뒤 할 것 (전부 코드 쪽, 클로드가 처리):
> - `.github/workflows/daily.yml` 에서 `WAFFLE_BASE_PATH` 와 `WAFFLE_SITE_URL` 두 줄 삭제
> - `src/build.py` 의 `render_cname` 주석 해제
> - Pages 설정에서 커스텀 도메인 지정 + HTTPS 강제
>
> **2. 검색 등록** — search.google.com/search-console, searchadvisor.naver.com
> 소유확인 값을 받아 `analytics.yaml` 에 넣는다. 국내 여행 검색은 네이버가 크다.
>
> **3. 구글 애널리틱스** — `G-` 로 시작하는 측정 ID 를 `analytics.yaml` 에.
> 넣으면 개인정보처리방침 문구가 자동으로 바뀐다(코드로 묶여 있다).
>
> **4. 괌·사이판·코타 해설 기사** — 이게 진짜 승부처다.
> 남의 보도가 없어 종합할 재료가 없고, 사장님 기록 없이는 영원히 0건이다.
> 사진은 이미 괌 48·사이판 9·코타 7장 있다. 필요한 건 숫자 서넛과 메모 한 줄.
>
> ### 지금 지면
>
> | 지역 | 데이터 | 큐레이션 | 해설 | 사진 |
> |---|---|---|---|---|
> | 괌 | 2 | 2 | **0** | 48 |
> | 사이판 | 2 | 1 | **0** | 9 |
> | 하와이 | 2 | 24 | 1 | 11 |
> | 베트남 | 2 | 39 | 1 | 8 |
> | 코타키나발루 | 2 | 1 | **0** | 7 |
> | 라오스 | 2 | 0 | 0 | 0 |
> | 제주 | 1 | 22 | 1 | 2 |
>
> 하와이·베트남·제주는 수집 기사가 들어와 종합 기사를 계속 낼 수 있다.
> 괌·사이판·코타는 수집 기사조차 없어 직접 취재분만 실을 수 있다.
>
> ### 매일 쓰는 명령
>
> ```bash
> python3 tools/admin.py          # 편집실 http://localhost:8080 (계정 불필요)
> .venv/bin/python tools/status.py  # 전체 상태와 막힌 곳
> ```
>
> 사진을 더 들여올 때는 NAS 를 연결하고(`smb://PEOPLAY`)
> `python3 tools/photo_prepare.py --region <지역> --from <폴더>` 로 검사한 뒤
> 콘택트시트를 **눈으로 보고** `--approve 3,7,12 --commit` 한다.
> 검출기의 '통과'는 믿을 수 없다 — 사람이 선 로비를 통과시킨 적이 있다.
>
> ### 건드리면 안 되는 것
>
> - `data/published_index.json` — 재발행을 막는 유일한 장치
> - 커밋 전 `bash tools/sync.sh` — CI 봇과 data/ 충돌을 막는다


작성일 2026-09-01 · 브랜치 `build/core-pipeline` · 테스트 236개 통과

---

## 1. 지금 무엇이 있나

7개 여행지(괌·사이판·하와이·베트남·코타키나발루·라오스·제주)의 여행 정보를 **매일 05:00 KST 에 자동 수집해 정적 신문 사이트로 발행하는 파이프라인**이 완성돼 있다.

```
sources.yaml (소스 33개, 활성 19개)
   │
   ├─ src/collect.py   20개 소스 병렬 수집 · 실패 격리        → data/raw/
   ├─ src/edit.py      등급 → 여행관련성 → 저작권 → 중복      → data/items/
   ├─ src/build.py     14일 윈도우 → 정적 HTML/RSS/sitemap   → public/
   └─ .github/workflows/daily.yml   매일 05시 + Pages 배포
```

**소스 구성**

| 종류 | 개수 | 비고 |
|---|---|---|
| 국내 여행 전문매체 | 8 | 여행신문·트래블데일리·투어코리아·트래블바이크뉴스·TTL뉴스·여행스케치·트래비·투어톡톡 |
| 여행 특화 해외매체 | 2 | VnExpress Travel, Beat of Hawaii |
| 지역 종합지 | 8 | 관련성 필터를 거쳐 여행 기사만 통과 |
| 환율 API | 1 | 6개 지역 (제주는 원화권) |

**콘텐츠 3등급**

- **A 사실 데이터** (환율) — 무검수, 매일 자동
- **B 큐레이션** (제목+2줄요약+원문링크) — 무검수, 매일 자동
- **C 해설 기사** (우리가 쓴 글) — 하루 5건 상한, 검수 후 발행

**A·B 만으로 사이트는 매일 갱신된다.** 손을 놔도 신문은 나간다.

---

## 2. 로컬에서 돌리기

```bash
cd ~/여행신문
.venv/bin/python -m src.collect && \
.venv/bin/python -m src.edit && \
.venv/bin/python -m src.publish_drafts && \
.venv/bin/python -m src.build && \
.venv/bin/python -m src.health
```

`publish_drafts` 는 승인된 해설 초안을 내보낸다. `edit` 가 쓴 오늘자 jsonl 에
**이어쓰므로** 반드시 편집 뒤·빌드 앞이어야 한다.

사이트 보기:
```bash
cd ~/여행신문/public && python3 -m http.server 8899
```
그다음 브라우저에서 `http://127.0.0.1:8899`

테스트:
```bash
cd ~/여행신문 && .venv/bin/python -m pytest -q
```

---

## 3. 배포하려면 — 사장님이 직접 하실 일

> **2026-09-01 갱신 — 3-1·3-2·3-3 은 완료됐다.**
> 저장소 `peoplay-k/waffletrip` 생성·공개 전환·푸시, Pages 활성화(build_type=workflow),
> 커스텀 도메인 `waffletrip.com` 등록, 워크플로 수동 실행까지 끝났고 **전 단계 통과**했다.
> 무료 플랜에서는 비공개 저장소에 Pages 를 붙일 수 없다는 것을 API 가 확인해줬다
> (422 "Your current plan does not support GitHub Pages") — 그래서 공개로 갔다.
>
> **남은 것은 3-4 카페24 DNS 하나뿐이다.** 지금 `waffletrip.com` 은 A레코드가 없어
> 어디에도 연결돼 있지 않고, 커스텀 도메인을 걸어둔 탓에 `peoplay-k.github.io/waffletrip`
> 도 그쪽으로 301 리다이렉트한다. DNS 를 넣는 순간 둘 다 살아난다.

### ★ 먼저 결정할 것: 저장소를 공개로 할 것인가 (결정됨 — 공개)

**GitHub 무료 플랜은 공개 저장소에서만 Pages 를 무료로 지원한다.** 비공개로 만들면 Pages 가 막히거나 유료 플랜(Pro 이상)이 필요하다.

- **공개** — Pages 무료. 코드와 수집한 헤드라인 데이터가 공개된다. 사이트 자체는 어차피 공개고, 데이터는 출처 표기된 헤드라인 모음이라 실질 노출은 크지 않다.
- **비공개 + 유료** — 월 비용 발생. "비용 0원" 설계와 어긋난다.

### 3-1. 저장소 생성과 푸시

공개로 갈 경우:
```bash
cd ~/여행신문 && ~/bin/gh repo create peoplay-k/waffletrip --public --source=. --remote=origin --push
```

### 3-2. Pages 활성화

GitHub 저장소 → **Settings → Pages → Build and deployment → Source** 를 **GitHub Actions** 로 변경. 이걸 안 하면 배포 잡이 실패한다.

### 3-3. 워크플로 수동 실행으로 확인

```bash
cd ~/여행신문 && ~/bin/gh workflow run "매일 발행" && sleep 20 && ~/bin/gh run watch
```

### 3-4. 카페24 DNS 연결

`waffletrip.com` 은 카페24 네임서버를 쓰고 있고 아직 아무 데도 연결돼 있지 않다. 카페24 DNS 관리에서:

| 타입 | 호스트 | 값 |
|---|---|---|
| A | @ | 185.199.108.153 |
| A | @ | 185.199.109.153 |
| A | @ | 185.199.110.153 |
| A | @ | 185.199.111.153 |
| CNAME | www | peoplay-k.github.io |

전파 후 **Settings → Pages → Custom domain** 에 `waffletrip.com` 입력, **Enforce HTTPS** 체크.

확인: `dig +short waffletrip.com A` → 185.199.10x.153 네 줄

---

## 4. 지금 채워야 할 것

### 4-1. 지역별 상품 링크

`src/render/site.py` 의 `PRODUCT_LINKS` 에 **괌만 채워져 있다.** 나머지 6개는 빈 값이라 상품 버튼이 안 나온다.

```python
PRODUCT_LINKS = {
    "guam": "https://guamplay.com",
    "saipan": "",      # ← 사이판플레이 사이트 주소
    "hawaii": "",      # ← 하와이플레이
    "vietnam": "",     # ← 베트남플레이
    "kota": "",        # ← 코타플레이
    "laos": "",        # ← 라오스플레이
    "jeju": "",        # ← 제주도플레이
}
```

**일부러 비워뒀다.** 전부 `guamplay.com` 으로 채워져 있던 것을 발견해 지웠다 — 하와이 페이지에 괌 여행사를 붙이는 것은 브랜드 격리 원칙 위반이다. 주소를 알려주시면 채운다.

### 4-2. 여행경보·날씨 소스

지금 A등급은 환율뿐이다. 외교부 여행경보와 날씨는 **인증키 발급**이 필요해 다음 사이클로 미뤘다. 공공데이터포털 키를 받으면 붙일 수 있다.

---

## 5. 알려진 한계 — 솔직히

### 5-1. 작은 목적지는 기사가 하루 1~2건이다

```
괌 2건 · 사이판 2건 · 하와이 20건 · 베트남 38건 · 코타 1건 · 라오스 1건 · 제주 12건
```

**이게 실제로 존재하는 양이다.** 괌 여행 뉴스는 하루에 열 건씩 나오지 않는다. 처음 100건처럼 보였던 것은 지역 선거·범죄 기사로 채워져 있었기 때문이고, 그건 걷어냈다.

지면을 채우려면 **긁어오는 게 아니라 우리가 써야 한다.** 그게 C등급 해설 기사 자리다.

### 5-2. 여행 관련성 필터는 완벽하지 않다

키워드 방식이라 양방향 오류가 난다. **놓치는 쪽으로 틀리게** 만들었다 — 여행 신문에 살인 기사가 실리는 것보다 여행 기사 몇 건을 놓치는 게 낫다.

알면서 놓치는 예: `"Guam Micronesia Island Fair"` (관광객이 갈 만한 행사인데 여행 단어가 없다. `fair` 를 키워드로 넣으면 공정성 기사가 함께 통과한다.)

### 5-3. C등급 발행 경로 — **해결됨** (브랜치 `fix/c-grade-publish`)

초안만 쌓이고 나가는 길이 없던 문제를 풀었다. 원인이 네 개였다.

1. **(해결)** 초안 파일명이 원본 기사 id 라 승인해도 중복 판정됐다
   → `src/publish_drafts.py` 의 `commentary_id()` 가 `c-` 접두사를 붙인다.
   원본 id 는 순수 16진수라 접두사가 있으면 절대 겹치지 않는다.
2. **(해결)** `apply_grades` 가 매 실행 등급을 덮어써 C가 B로 강등됐다
   → `if item.grade != "C"` 로 막았다.
3. **(해결)** 발행 경로 자체가 없었다 — `approved` 를 읽는 코드가 어디에도 없었다
   → `src/publish_drafts.py` 신규. 파이프라인이 `collect → edit → **publish_drafts** → build` 가 됐다.
4. **(해결)** 템플릿이 `body_md` 를 이스케이프해 마크다운이 원문 그대로 보였다
   → `src/render/md.py` 신규 + `|md` 필터. **이스케이프를 먼저 하고 구조를 나중에 넣는다.**

추가로 **중복 가드가 해설을 막던 문제**도 함께 풀었다. 해설은 원본 사건을 일부러
다시 다루므로 제목이 겹치는 것이 정상인데, 제목 유사도 판정에 걸려 우리가 쓴 글이
우리가 실은 원문에 막혔다. A등급과 같이 **id 로만** 판정하게 했다.

그리고 발행된 초안이 48시간 뒤 삭제되던 문제를 막았다 (`purge_stale_drafts` 가
`approved` 만 지키고 `published` 는 안 지켰다).

**개인정보 가드가 발행 경로에 붙었다** (`src/guards/privacy_guard.py`).
해설 기사는 여행사 운영 기록에서 나오고 그 원천에는 고객 개인정보와 거래처 계약
숫자가 섞여 있다. 주민번호·여권·휴대전화·카드번호·이메일·예약번호·고객 실명과
거래단가 용어를 발행 직전에 막는다. **놓치는 쪽이 아니라 막는 쪽으로 틀리게** 했다.

### 5-4. 실패 알림 — **해결됨** (브랜치 `fix/c-grade-publish`)

`src/health.py` 를 붙였다. GitHub Actions 는 잡이 **실패해야** 메일을 보내므로,
이상을 발견하면 조용히 넘어가지 않고 `exit 1` 로 죽는다.

| 증상 | 판정 | 근거 |
|---|---|---|
| 오늘 수집 0건 | 치명 · 잡 실패 | 전 소스가 막혔거나 네트워크가 죽었다 |
| 3일 연속 발행 0건 | 치명 · 잡 실패 | 6-2 의 "기사가 점점 안 실린다"를 잡는 장치 |
| 같은 소스 3일 연속 실패 | 경고만 | 소스 하나가 죽어도 신문은 나가야 한다 |

기록은 `data/health.json` 에 최근 30일치가 남는다. 빌드 **뒤에** 두었다 —
오늘치 사이트는 일단 내보내고 나서 경보를 울린다.

직접 돌려보기: `.venv/bin/python -m src.health`

---

## 6. 운영하다 터진다면 여기서 터진다

최종 리뷰가 짚은 세 곳이다.

1. **스케줄 실행의 침묵** — 05시에 아무 일도 안 일어나는 것을 아무도 모른다. 실패 알림·연속실패 감시가 없다.
2. **중복 가드가 정상 발행까지 막는 방향** — 발행 이력이 커질수록 제목 유사도 판정이 공격적이 된다. 증상이 "에러"가 아니라 "기사가 점점 안 실린다"로 나타난다. 한 번 이력에 들어간 id 는 영구다.
3. **저장소가 곧 데이터베이스** — 발행 이력과 기사 목록이 매 실행 커밋·푸시에 의존한다. 푸시가 실패해도 배포는 이미 끝난 뒤다.

---

## 6-A. 사진 파이프라인 (2026-09-01 추가)

NAS(`/Volumes/GUAMPLAY`, SMB, 사진 26,716장)의 사진을 기사에 붙이는 경로다.
**NAS 가 마운트돼 있지 않아도 도구는 만들어 뒀다.** 연결되면 바로 돌아간다.

```bash
# ① NAS 를 폴더별로 훑어 인덱스를 만든다 (전체 순회는 SMB 에서 조용히 끊긴다)
python3 tools/nas_index.py /Volumes/GUAMPLAY
python3 tools/nas_index.py /Volumes/GUAMPLAY --shallow    # 연결 확인만

# ② 검사하고 콘택트시트를 뽑는다 (아직 아무것도 굽지 않는다)
python3 tools/photo_prepare.py --region guam --from /Volumes/GUAMPLAY/#DSLR/#괌

# ③ 시트를 눈으로 보고, 쓸 번호만 골라 굽는다
python3 tools/photo_prepare.py --region guam --from ... --approve 3,7,12-15 --commit
```

`cv2` 가 필요해 **시스템 파이썬(`python3`)으로 돌린다.** venv 에 넣지 않은 이유는
이 도구가 로컬 운영용이고 CI 는 사진을 굽지 않기 때문이다 —
requirements.txt 에 opencv 를 넣으면 매일 05시 잡이 무겁게 무거워진다.

### 왜 자동 통과로 굽지 않는가

**검출기의 '통과'는 믿을 수 없다.** 실측에서 확인한 것:

- 거의 같은 사진의 밝기 보정본끼리 판정이 갈렸다 (하나는 통과, 하나는 얼굴 2건)
- 통과한 쪽에도 **로비에 사람이 서 있었다**
- 민감도를 올리자 로브스터 사진에서 "얼굴 68건"이 나왔다. 87%를 막으면
  사람이 어차피 전부 다시 봐야 해서 도구가 무의미해진다

그래서 설계를 바꿨다. **검출기는 게이트가 아니라 표식이고, 진짜 안전장치는
사람 승인이다.** 여기에 값싼 보강을 하나 얹었다 —
**한 장면의 변형 중 하나라도 걸리면 그 장면을 통째로 막는다.**
위의 로비 사진이 이걸로 잡혔다.

### 지켜야 할 것

| 규칙 | 이유 |
|---|---|
| EXIF 회전 먼저, 그 뒤 4방향 검사 | 눕혀 저장된 사진에서 검출기가 0점을 준다 |
| 콘택트시트를 항상 눈으로 본다 | 자동 검출은 보조 수단이다 |
| `data/photos/sheets/` 는 커밋하지 않는다 | **얼굴이 그대로 들어 있고 저장소가 공개다** |
| `.gitignore` 주석은 제 줄에 쓴다 | 줄 끝 주석은 지원되지 않는다. 규칙이 통째로 무효가 돼 시트가 올라갈 뻔했다 |
| 지역은 추측하지 않는다 | 경로에서 확실할 때만 태깅. 하와이 기사에 괌 사진이 실리면 아무도 눈치채지 못한다 |

## 7. 파일 위치

```
~/여행신문/
├── 인수인계.md                          ← 이 문서
├── sources.yaml                         소스 레지스트리
├── src/                                 파이프라인
├── tests/                               236개
├── public/                              빌드된 사이트
├── data/items/                          발행 기사 (커밋됨)
├── data/published_index.json            ★발행 이력 — 손대지 말 것
├── content/review/                      C등급 검수 초안
├── .github/workflows/daily.yml          자동 실행
└── docs/superpowers/
    ├── specs/2026-08-31-play-travel-news-design.md    설계
    └── plans/2026-08-31-waffletrip-core.md            구현 계획
```

**`data/published_index.json` 은 재발행을 막는 유일한 장치다.** 지우면 어제 낸 기사를 오늘 또 낸다.

---

## 8. 제가 사장님 대신 내린 판단 전부

작업 중 막히면 멈추는 대신 판단하고 계속 갔습니다. 아래가 그 전부이고,
각각 **틀렸을 때 무슨 비용이 드는지**까지 적었습니다. 마음에 안 드는 게 있으면
되돌릴 수 있습니다.

**1.** 별도 worktree 대신 main 체크아웃 안의 브랜치에서 작업한다 — 저장소가 오늘 만든 비공개 신규 저장소라 격리할 병행 작업이 없고, 사용자가 빌드 결과를 ~/여행신문/public/index.html 고정 경로에서 열어봐야 한다. 틀렸을 때 비용: 낮음 (병합이 fast-forward).

**2.** 계획서에서 임포트를 `date, timedelta` 로 줄였다 — 리뷰어가 잡을 죽은 임포트를 구현 전에 없앤다. 틀렸을 때 비용: 없음.

**3.** `save()` 에서 id 로 접도록 2줄 고치고 그 동작을 고정하는 테스트를 추가했다 (T7 테스트 13→14개). 파킹하지 않은 이유는 수정이 파킹보다 싸기 때문. 틀렸을 때 비용: 낮음 (유사도 비교 대상 수만 달라짐).

**4.** saipan·kota 두 지역 뉴스 소스 0개를 이번 사이클에서 메우지 않는다 — 후보 4개가 전부 robots 전면차단(mvariety) 또는 Cloudflare 봇차단 403(borneopost, dailyexpress) 또는 서버 불안정(saipantribune)이라 우회가 곧 규칙 위반이거나 불안정 소스 투입이다. 대신 Task 13 이후 별도 '소스 확장' 작업으로 Google News RSS 검색피드 (news.google.com/rss/search?q=… — 신디케이션용으로 공개된 무료 피드)를 붙여 메운다. 지금 sources.yaml 을 손대지 않는 이유: 소스 추가는 yaml 한 줄이라 코드 변경이 없고, 지금 바꾸면 Task 1 리뷰가 무효가 된다. 틀렸을 때 비용: 두 지역 페이지가 환율만 있는 빈 페이지로 한동안 남는다 (Task 10 이 빈 지역도 정상 렌더하도록 이미 설계됨).

**5.** guam_post(postguam.com) 는 HTTP 429 보류 상태로 둔다 — 조사 트래픽이 레이트리밋을 건드렸을 가능성이 높아 죽은 소스로 확정하지 않는다. 위 '소스 확장' 작업에서 시간 두고 재조사한다. 틀렸을 때 비용: 괌 소스가 1개로 유지된다.

**6.** 국내 여행 전문 매체를 주력 소스로 전환한다. 현지 영자매체는 보조로 남긴다. 근거: 현지 매체는 여행 기사 비율이 낮고 사이판·코타는 봇차단으로 접근 불가인데, 국내 여행지는 전 기사가 여행 기사이고 막힌 지역도 다룬다. 틀렸을 때 비용: 현지 1차 정보(현지 행사·규정 변경)의 즉시성이 떨어진다 — 보조 소스가 메운다.

**7.** 이 피드는 목적지가 섞여 있어 소스에 지역을 고정할 수 없다. region: auto 를 도입하고 src/region_tag.py 가 기사마다 지역을 추론한다. Task 2 에 태거를 넣었다(순수 함수라 모델과 같은 단위). Task 3 이 auto 소스를 태깅하고 미매칭 기사는 버린다. 틀렸을 때 비용: 태거 오탐이 곧 오보다 — 그래서 광역 지명 대신 도시·섬 이름으로만 매칭한다.

**8.** 한 기사에 여러 지역이 나오면 언급 횟수가 많은 쪽 하나로 정하고, 동점이면 REGIONS 고정 순서로 깬다. 지역별 복수 게재를 하지 않는 이유는 id 가 URL 기준이라 같은 기사를 두 항목으로 만들면 중복 가드가 흔들리기 때문. 틀렸을 때 비용: '괌·사이판 공동 프로모션' 같은 기사가 한쪽 페이지에만 실린다.

**9.** 지역 미매칭 기사(오사카·파리 등)는 버린다. 별도 '여행업계 공통' 섹션을 지금 만들지 않는 이유는 정보구조에 8번째 버킷을 추가하는 일이라 네비·사이트맵·템플릿을 함께 건드려야 하고, 코어가 도는 것을 먼저 봐야 하기 때문. 틀렸을 때 비용: 유류할증료·여권 같은 전 지역 공통 소식을 당분간 못 싣는다. Task 13 이후 별도 작업으로 추가.

**10.** 앞서 내린 "saipan·kota 를 이번 사이클에서 메우지 않는다" 를 **철회**한다. 근거가 된 조사가 불완전했다. 수정 라운드에서 스펙의 누락 후보 8개를 마저 조사한다. 이것이 리뷰를 붙이는 이유다.

**11.** 계획서 Task 1 Step5 문구 "실용데이터 소스 두 개" 를 "환율 하나"로 정정했다 — 코드블록에 하나뿐이라 지문이 틀렸다. 날씨·여행경보는 인증키가 필요해 다음 사이클. 틀렸을 때 비용: 없음(의도 명확화).

**12.** saipan_tribune 은 신뢰도가 낮지만(6회 중 2회 성공) 등록을 유지한다 — collect.py 가 소스 실패를 항목 단위로 격리하도록 이미 설계돼 있어(Task 5) 불안정 소스가 전체를 막지 않는다. 사이판의 유일한 현지 소스라 빼면 그 지역이 국내매체에만 의존하게 된다. 틀렸을 때 비용: _errors.json 에 실패가 자주 찍힌다(3일 연속 실패 감시가 잡아낸다).

**13.** 구현자가 찾은 계획서 모순은 실재한다 — test_no_keyword_is_a_single_character 가 "모든 키워드 2글자 이상"을 요구하는데 '괌'은 1글자다. 내 계획서 결함이다. 길이 규칙을 **명시 허용목록**(SINGLE_CHAR_ALLOWED = {"괌"})으로 바꾸고, 진짜 방어선인 오탐 회귀 테스트(오사카·파리·도쿄·방콕·세부·유류할증료·여권·발리 → 모두 None)를 추가했다. '괌'을 뺄 수도 있었지만 그러면 우리 주력 지역을 못 잡는다. 실측으로 오탐 0 확인함. 틀렸을 때 비용: '괌'을 포함한 미지의 한국어 단어가 오탐을 낸다 — 회귀 테스트가 방어선.

**14.** 구현자가 넣은 `from __future__ import annotations` 는 이탈이 아니라 필수다 — 로컬 파이썬이 3.9.6 이라 `str | None` 런타임 평가가 깨진다. 전역 제약에 "Python 3.9 호환"을 추가해 이후 태스크가 3.10+ 문법을 런타임에 쓰지 않게 했다. 틀렸을 때 비용: 없음(CI 는 3.11 이라 어느 쪽이든 돈다).

**15.** 구현자가 찾은 계획서 모순은 실재한다 — test_no_keyword_is_a_single_character 가 "모든 키워드 2글자 이상"을 요구하는데 '괌'은 1글자다. 내 계획서 결함이다. 길이 규칙을 명시 허용목록(SINGLE_CHAR_ALLOWED={"괌"})으로 바꾸고, 진짜 방어선인 오탐 회귀 테스트(오사카·파리·도쿄·방콕·세부·유류할증료·여권·발리 → 전부 None)를 추가. '괌'을 빼는 선택지도 있었으나 그러면 주력 지역을 못 잡는다. 오탐 0 을 실측 확인함. 틀렸을 때 비용: '괌'을 포함한 미지의 한국어 단어가 오탐 — 회귀 테스트가 방어선.

**16.** 구현자의 `from __future__ import annotations` 는 이탈이 아니라 필수다 — 로컬 파이썬 3.9.6 에서 `str | None` 런타임 평가가 깨진다. 전역 제약에 "Python 3.9 호환" 추가해 이후 태스크가 3.10+ 문법을 런타임에 쓰지 않게 고정했다. 틀렸을 때 비용: 없음.

**17.** Task 3 부터 구현자 모델을 haiku → sonnet 으로 올린다. haiku 가 완전한 코드 전사에서 한글 오타를 냈고("오아후"→"오아부") 그 한 글자가 하와이 기사를 통째로 놓치게 만들었다. 수정 라운드 한 번의 비용(구현자+리뷰어+재리뷰어)이 모델 차액보다 크다. 앞으로 태스크는 한글 키워드·테스트 문자열이 계속 나오므로 전사 정확도가 곧 정확성이다. 틀렸을 때 비용: 토큰 비용이 다소 늘어난다.

**18.** 문장 분리 규칙에 "한글 바로 뒤 종결부호"를 추가한다. 앞이 한글일 때만 걸리므로 영문 약어에 영향이 없다. 실측 재확인: 폐기율 70% → 20%. 틀렸을 때 비용: 요약이 의도보다 짧아질 수 있다(저작권상 안전한 방향).

**19.** REGION_EXCLUSIONS 를 도입해 세기 전에 지운다. 재확인: 오탐 3건 → 0건, 진짜 제주 기사 3건은 그대로 jeju. 틀렸을 때 비용: 제주항공이 실제로 제주 노선을 다루는 기사를 놓칠 수 있다(그런 기사는 보통 제목에 "제주 노선"이 따로 나온다).

**20.** **제목에서만 판정한다.** 계획서의 test_auto_source_uses_summary_when_title_has_no_place_name 을 정반대 테스트로 교체했다(요약에만 있는 지명은 무시). 요약에는 다른 목적지가 스쳐 지나가고, 소스에 따라 아예 다른 기사 본문이 실려 오기도 한다. 틀렸을 때 비용: 제목에 목적지를 안 쓴 기사를 놓친다 — 수집량이 20건에서 10건으로 준다. 오보를 내느니 적게 싣는 쪽을 택한다.

**21.** 계획서에 있던 "약어의 마침표는 살아남는다"는 주석은 처음부터 사실이 아니었다 (원래 규칙도 ". " 에서 잘랐다). 주석을 사실대로 고치고 테스트로 그 동작을 고정했다. 틀렸을 때 비용: 영문 피드 요약이 다소 짧아진다.

**22.** 구현자가 "브리프가 이전 라운드 수정을 계속 누락한다"고 지적한 것이 맞다. 대화로만 지시한 수정(하와이 철자 회귀테스트, future import)이 계획서에 반영되지 않아 브리프를 새로 뽑을 때마다 사라졌다. 계획서 13개 모듈 코드블록에 future import 를, Task 2 에 하와이 회귀테스트를 역이식했다. 계획서가 정본이어야 브리프가 정본이 된다. 틀렸을 때 비용: 없음(이후 태스크에서 같은 표류가 반복되는 것을 막는다).

**23.** 하와이안항공을 REGION_EXCLUSIONS 에 추가한다. 재리뷰어가 실증한 오탐이고 Task 3 이 이번 라운드에 건드린 파일 범위 안이라 파킹하지 않고 잡는다. 단 국적 항공사 (베트남항공·라오항공)는 넣지 않는다 — 그 항공사 소식은 거의 항상 자국 관련이라 지우면 진짜 기사를 잃는다. 검증: 나리타 기사 None, 호놀룰루/하와이노선 기사 hawaii 유지, 베트남항공 영향 없음. 틀렸을 때 비용: "하와이안항공 취항 30주년" 처럼 지명 없는 항공사 자체 기사를 놓친다.

**24.** 구현자가 "task-2-brief.md 가 실제로는 갱신 안 됐다"고 지적한 것이 맞다 — 계획서만 고치고 브리프 재생성을 빠뜨렸다. 계획서 수정 뒤에는 반드시 브리프를 다시 뽑는다. 지금 Task 2·3 브리프를 재생성했다. 틀렸을 때 비용: 없음(절차 교정).

**25.** 구현자가 올린 "1 VND = 약 0원" 은 표시 오류가 아니라 오보다. 라이브 확인: 1 VND=0.0529원, 1 LAK=0.0619원이라 {:,.0f} 로는 둘 다 0원이 된다. 베트남·라오스 페이지의 환율 패널이 통째로 무의미해진다. 은행 고시 방식대로 QUOTE_UNIT 을 도입해 저액면 통화를 100단위로 묶고, 100원 미만은 소수 한 자리를 보여준다. 실물 재확인: 6개 지역 전부 정상값. 틀렸을 때 비용: 없음(기존 USD/MYR 표시는 그대로 1단위 유지).

**26.** 기존 테스트 9개가 전부 통과하는데도 이 버그가 살아남은 이유는 테스트가 USD 사례만 검증했기 때문이다. 저액면 통화 3케이스를 테스트로 못박았다. 앞으로도 "대표값 하나만 테스트하는" 패턴을 리뷰에서 볼 것.

**27.** 리뷰어의 fail-fast(모듈 로드 시 RuntimeError) 제안을 채택하지 않았다. 리뷰어가 모르는 정보가 있다 — Task 5 수집 오케스트레이터가 소스별 예외를 격리하므로, 모듈 로드 시점에 던지면 환율 설정 실수 하나가 RSS 수집까지 죽인다. 대신 테스트로만 잡는다 (개발 시점에 잡히고, 운영에서는 격리 장치가 받는다). .get(c,1) 조용한 기본값은 리뷰어 경고대로 쓰지 않았다 — 미래 저액면 통화에서 "0원" 오보 재발. 틀렸을 때 비용: 통화 추가 실수 시 그 소스만 하루 빠지고 _errors.json 에 남는다.

**28.** Minor "krw >= 100 경계"(99.96→"100.0원", 100.04→"100원")는 파킹. 현재 4개 통화 실측값(1,377/312/5.3/6.4)이 경계 근처에 없다. 최종 리뷰에서 triage.

**29.** Task 6(저작권 가드)과 Task 7(중복 가드)을 한 디스패치로 묶는다. 둘 다 src/guards/ 의 순수 검증 모듈이고 서로 의존하지 않으며 파일시스템을 모른다(인덱스 경로는 주입받는다). 계획서에 완전한 코드가 있어 전사+검증 작업이라 판단 분기가 적다. 왕복 절반 절감. 틀렸을 때 비용: 리뷰가 두 모듈을 한 번에 봐야 해 수정 라운드가 생기면 churn 이 커진다.

**30.** 구현자의 연쇄 비교(chain linking) 수정을 승인한다. 계획서 코드가 대표의 원제목하고만 비교해 자기 테스트(test_similar_titles_collapse_and_record_related, 사슬형 3건)를 통과하지 못하는 상태였다. 내 계획서 결함이다. 계획서를 구현자 방향으로 고쳤다. 틀렸을 때 비용: 단일연결 클러스터링은 사슬로 엮일 수 있다 — 실측 254건에서 최대 클러스터 크기 4, 임계값 0.7 에서 폭주 없음을 확인.

**31.** 클러스터링이 지역·등급을 무시해 지역별 환율을 삼켰다. 괌·사이판·하와이는 같은 USD 를 써서 제목이 글자 그대로 같고, 클러스터가 "같은 사건을 세 매체가 보도"로 착각했다. 실측: 환율 6건 → 4건, 사라진 둘이 사이판·하와이. 두 원칙으로 고쳤다 — ①A등급(우리가 만든 사실 데이터)은 클러스터 대상이 아니다 ②지역이 다르면 같은 사건일 수 없다. 검증 후: 환율 6건 전부 생존, 태풍속보 4건 묶음은 유지. 틀렸을 때 비용: 지역을 넘나드는 진짜 공동 사건("괌·사이판 공동 프로모션")이 두 번 실린다 — 단 우리는 항목당 지역을 하나로 확정하므로 그런 항목은 애초에 한 지역에만 있다.

**32.** copyright_guard 가 약어의 마침표를 문장 끝으로 세어 멀쩡한 기사를 폐기한다. "The U.S. Embassy issued a statement today. Details follow." → 3문장으로 계산. 치환 방식으로 약어를 가린다(파이썬 정규식이 가변길이 lookbehind 를 지원하지 않아 "약어가 아닌 마침표"를 한 패턴으로 못 쓴다). 목록은 불완전하지만 놓치면 폐기 쪽으로 틀리므로 안전한 방향. 검증: 7케이스 전부 기대값 일치. 틀렸을 때 비용: 문장 끝 단독 대문자("The grade was A. Next...")를 1문장으로 셈 — 통과시키는 쪽 오류라 200자 상한이 받는다.

**33.** title_tokens 가 한 자리 숫자를 버려 "Update 5"와 "Update 1"의 토큰이 같아지고(유사도 1.00) 순차 속보가 병합됐다. 두 자리(11·12호)는 분리되던 비일관. 숫자를 예외로 남긴다. 검증: 1.00→0.67 로 11·12호와 같아짐, 기존 테스트 보존. 실물: 태풍 속보 4건이 정상 분리(대표 250→253). 틀렸을 때 비용: 순차 속보가 각각 실린다 — 각 속보에 새 정보가 있으므로 의도된 동작.

**34.** PublishedIndex.load 가 타입 틀린 JSON 을 조용히 통과시켰다 ({"ids":"not-a-list"} → 문자열이 글자 set 으로 분해). fail-closed 원칙의 틈이라 막았다.

**35.** 구현자의 테스트 문자열 교체("A. B. C." → 실제 단어) 승인. _INITIAL 이 대문자 한 글자+마침표를 이니셜로 가리는 것은 의도된 동작이고 진짜 이니셜과 구분 불가다. 다만 쌍둥이 테스트가 함께 약해진 것을 내가 잡아 라운드 3 으로 고쳤다 — "A. B."가 1문장으로 세어지는데 이름은 "정확히 2문장 경계"를 주장하고 있었다. 검증: 이제 2·3 을 실제로 센다.

**36.** Task 8(등급 분류)과 Task 9(편집 오케스트레이터)를 한 디스패치로 묶는다. 9가 8을 소비하는 순차 의존이라 한 에이전트가 순서대로 하면 인터페이스 어긋남이 없다. 틀렸을 때 비용: 리뷰가 두 모듈을 한 번에 봐야 한다.

**37.** FLIGHT_KEYWORDS 의 "launch" 가 부분일치해 군사 기사 "U.S. forces strike two Iranian launchers on Iran's Larak island" 가 항공 섹션으로 올라오고 검수 후보까지 됐다. 여행 신문 신뢰도가 걸린 문제다. 영문 키워드는 단어경계 + 어미 허용(-s/-es/-ed/-ing)으로, 한글은 부분일치 유지(조사가 붙어 오므로). 검증 9케이스 전부 기대값 일치, 실물에서 빠지는 것이 정확히 그 이란 기사 하나. 틀렸을 때 비용: 흔치 않은 어형("relaunching" 등)을 놓칠 수 있다.

**38.** 구현자 우려2("폐기 97건이 전부 200자 초과 한 사유")는 의도된 동작이라 미수정. first_sentences 는 앞 2문장만 뽑고 글자수를 자르지 않는다 — 자르면 남의 글 앞부분을 그대로 싣는 것과 같다. 2문장이 200자를 넘으면 요약 실패로 보고 폐기한다. 폐기율 28% 정상.

**39.** write_drafts 가 프런트매터를 f-string 으로 써서 제목의 콜론이 YAML 을 깨뜨렸다. 실측 351건 중 37건이 콜론 제목("DPS: 76-year-old man died..."). 터지면 검수 큐가 통째로 안 열린다. yaml.safe_dump + allow_unicode(한글 보존) + sort_keys=False(사람이 읽는 순서 유지)로 고쳤다. 검증: 위험 37건이 0/37 → 37/37 파싱. PyYAML 은 이미 고정 의존성이라 새 의존성 아님. 틀렸을 때 비용: 없음.

**40.** Ruling(Important x4, 전부 브리프 코드 결함 — 우리가 남의 사이트 텍스트·URL 을 그대로 렌더한다는 전제에서 오는 구멍들): ① source_url 에 javascript: 스킴이 들어오면 클릭 가능한 링크가 생성됐다. autoescape 는 HTML 특수문자만 막고 URI 스킴을 안 거른다. safe_url 화이트리스트(http/https)를 Jinja 필터로 등록했다. 링크가 위험하면 출처명은 남기고 링크만 뺀다(저작권 표기 유지). ② article_url 이 id·region 을 정제 없이 경로에 넣어 out_dir 밖에 파일이 써졌다 (리뷰어가 디스크에서 확인). _SAFE_SEGMENT 로 정제. 검증: "../../../../evil"→"evil". ③ _rfc822 가 ValueError 만 잡아 published_at=None 이면 TypeError 로 피드 전체가 죽었다. TypeError 도 잡고 str(iso) 로 폴백. ④ 제목의 XML 제어문자 하나가 rss.xml 전체를 재파싱 불가로 만들었다. _xml_safe 로 제거. 틀렸을 때 비용: ①은 http/https 외 스킴의 정상 링크를 잃는다(RSS 소스에 사실상 없음).

**41.** 리뷰어 지적대로 기존 테스트가 "긁어온 제목의 HTML 이스케이프"를 하나도 단언하지 않아 네 결함이 살아남았다. 보안 테스트 7개를 추가했다.

**42.** load_recent_items 가 14일치 jsonl 을 이어붙이며 id 중복을 안 걸러 모든 기사가 두 번씩 실렸다(괌 페이지 20줄 = 고유 10건 x 2). 테스트 211개가 전부 통과하는 상태였다 — 완성된 사이트를 실제로 열어봐야 보이는 결함이었다. 정렬 뒤 id 기준 중복 제거(최신 판본 유지). 검증: 277건 = 고유 277개.

**43.** 계획서 test_travel_articles_pass 가 "fair" 를 키워드에서 뺀 상태로 "Guam Micronesia Island Fair" 통과를 기대해 모순이었다. 키워드가 아니라 테스트를 고쳤다 ("fair" 를 넣으면 공정성·박람회 기사가 통과). 대신 그 누락을 문서화하는 테스트 test_known_misses_are_documented 를 추가했다. 놓치는 쪽으로 틀리는 게 낫다. 틀렸을 때 비용: 여행 단어 없는 지역 축제 기사를 놓친다.

**44.** 구현자가 찾은 travel⊂travelling 부분일치 오탐("Sabah tour bus accident")은 유보. travel 은 이 필터의 핵심 키워드라 빼면 대부분을 놓치고, 해당 기사도 여행자 안전 정보로는 경계선이다. 다음 사이클에서 단어 경계 적용 검토(항공 키워드 방식).

**45.** 리뷰가 찾은 한국어 부분일치 오탐(여권⊂여권통문, 공항⊂제2공항·한국공항공사)을 TRAVEL_EXCLUSIONS 로 막고, 영문은 단어경계+어미로 바꿨다(항공 키워드와 같은 방식). 검증 중 별개 누락도 발견 — "Korean Air launched a new route" 가 안 잡혀 route 를 추가. 소급 정리 2차로 5건 추가 제거(4건이 리뷰 지적과 일치). 틀렸을 때 비용: 제외 목록이 불완전해 유사 오탐이 또 나올 수 있다.

**46.** 구현자가 보고한 travelled 테스트 실패는 테스트가 맞고 코드가 부족한 것이었다. travelling·travelled 는 영국식 철자이고 말레이시아·싱가포르·호주 영어권 소스의 표준이다 (우리 소스 중 New Straits Times 가 그쪽). _INFLECTION 에 겹자음을 허용했다. 검증 9케이스 전부 일치, 새 오탐 없음.

**47.** Task 14 라운드 3 의 범위한정 재리뷰를 생략하고 최종 전체 리뷰로 대체한다. 한 줄 정규식 변경이고 내가 9케이스로 직접 검증했으며, 최종 리뷰가 어차피 전 브랜치를 본다. 틀렸을 때 비용: 낮음(변경 범위가 한 줄).
